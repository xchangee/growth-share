interface:
  display_name: "Open Creative Wall"
  short_description: "安全定制、重新生成合成资料并验收开放创意墙"
  default_prompt: "Use $open-creative-wall to customize this event-wall template with synthetic-only data, validate it in a browser, and stop before publishing."

policy:
  allow_implicit_invocation: true
